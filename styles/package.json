{
    "devDependencies": {
        "cssnano": "^7.0.5",
        "clean-css-cli": "^5.6.3",
        "rimraf": "^6.0.1",
        "sass": "^1.77.8"
    },
    "scripts": {
        "clean": "rimraf dist",
        "build": "sass src/style.scss:dist/lnb.style.css --no-source-map",
        "minify": "cleancss -o dist/lnb.min.css dist/lnb.style.css",
        "start": "npm run clean && npm run build && npm run minify"
    },
    "dependencies": {
        "bootstrap": "^5.3.3"
    }
}