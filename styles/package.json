{
    "devDependencies": {
        "cssnano": "^7.0.5",
        "postcss-cli": "^11.0.0",
        "sass": "^1.77.8"
    },
    "scripts": {
        "build": "sass src/:dist/",
        "minify": "postcss dist/*.css --use cssnano --dir dist/",
        "start": "npm run build && npm run minify"
    },
    "dependencies": {
        "bootstrap": "^5.3.3"
    }
}
